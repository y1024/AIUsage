---
description: How to diagnose UI stutter / slowness in this macOS SwiftUI app — profile with Instruments first, never guess
alwaysApply: false
---

# 性能排查：先用 Instruments 抓证据，不要猜代码

卡顿/滑动掉帧/CPU 高时，**先拿真实 profile 再定位**。详细方法论与命令见 `docs/PERFORMANCE_PROFILING.md`。

## 最短路径

```bash
# 1. 附加 Time Profiler 到运行中的 app（用户在此窗口里复现卡顿）
xctrace record --template "Time Profiler" --attach <pid> --time-limit 50s --output /tmp/p.trace
# 2. 导出主线程样本表
xctrace export --input /tmp/p.trace --xpath '/trace-toc/run[@number="1"]/data/table[@schema="time-profile"]' > /tmp/tp.xml
# 3. 解析 XML（线程/帧用 ref 复用，需解 ref），按符号聚合 Main Thread 自耗时 + 总耗时，排序看头号热点
#    也查 potential-hangs (>250ms) 与 hang-risks 两张表
```

## 定位与修复要点

- **关注主线程**：滑动卡顿 = 主线程被阻塞。看 `time-profile` 里 `Main Thread` 的符号聚合，不是总 CPU。
- **热点常在意料之外**：卡顿源可能与当前页面无关（如常驻/定时刷新的协调器、被多视图读取的计算属性）。以 profile 为准。
- **修复优先级（对应 main.mdc 规则#9：高成本对象只建一次复用）**：
  - 高成本对象（`NSRegularExpression`、大字典、`DateFormatter`）→ 静态常量 / `NSCache` 缓存。
  - 被多个视图 body 反复读取的昂贵计算属性 → 失效式记忆化（输入源 `objectWillChange` 置脏，读取命中缓存）。
  - 主线程文件系统调用（`resolvingSymlinksInPath`/`fileExists`/`lstat`）→ 缓存结果或移出主线程。
- **改完必须重抓 profile 对比前后**，用同一聚合脚本验证热点确实下降，而不是凭感觉。

## 反例

- ❌ 看到「卡」就去改最近改过的视图 / 加 `.drawingGroup()` / 拆 `LazyVStack`，没有 profile 支撑。
- ✅ 先 attach Time Profiler，按符号聚合主线程耗时，命中根因后再针对性缓存。
